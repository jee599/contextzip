# X/Twitter Daily Post Schedule

Cowork에 넣을 내용. 두 프로젝트 교차 홍보.

## Cowork Prompt (기존 거 이걸로 교체)

```
Chrome을 열어서 x.com에 접속해. 로그인 되어있을 거야.
아래 글 중 오늘 날짜에 해당하는 글을 트윗으로 올려줘.

3/21: Building in public. First project: ContextZip — compresses CLI output by 60-90% for AI coding assistants. Rust. 1,056 tests. One command install. github.com/jee599/contextzip

3/22: npm install dumps 150 lines of deprecated warnings into your AI context window. ContextZip turns it into 3 lines. Security warnings kept. npx contextzip github.com/jee599/contextzip

3/23: Claude Code Agent Teams is powerful. But it doesn't know WHICH agents to use. AgentCrow auto-matches from 9 builtin + external agents. One command: npx agentcrow init github.com/jee599/agentcrow

3/24: 🧵 Claude Code keeps forgetting my code. Not a bug. npm install, Docker build, stacktraces — all dumping noise into the context window. I built ContextZip to fix it. npx contextzip github.com/jee599/contextzip

3/25: "Build a landing page" → AgentCrow auto-decomposes into 4 subtasks, picks the right agent for each, dispatches in parallel. No manual prompt splitting. npx agentcrow init github.com/jee599/agentcrow

3/26: Docker build output: 50 lines of layer hashes. ContextZip: 1 line. "✓ built my-app:latest (12 steps, 8 cached)" github.com/jee599/contextzip

3/27: Two tools I built for Claude Code: ContextZip — compress noisy CLI output (60-90% savings). AgentCrow — auto-dispatch specialized agents (9 builtin). Both: npx install, zero config. github.com/jee599/contextzip github.com/jee599/agentcrow

3/28: 102 real benchmarks for ContextZip. Best: 97% saved. Worst: -12% (output grew). We show both because hiding failures is dishonest. github.com/jee599/contextzip

3/29: AgentCrow agents: frontend-dev, backend-dev, qa-tester, devops, security-auditor, tech-writer, code-reviewer, refactor-specialist, performance-optimizer. All auto-matched to your prompt. npx agentcrow init github.com/jee599/agentcrow

3/30: Python traceback with 20 Flask framework frames? ContextZip keeps your code, hides the framework noise. Works for Node.js, Python, Rust, Go, Java. npx contextzip github.com/jee599/contextzip

올린 후 타임라인에서 AI나 개발 관련 트윗 3개에 좋아요 누르고, 1개에 짧은 리플라이 달아줘.
```
